export function CompleteProfilePage() {
  // TODO: shown after a Google-first signup when profileCompletedAt is null; collects
  // USTA rating + registration code via POST /api/auth/complete-profile.
  return (
    <div>
      <h1>Complete your profile</h1>
      <p>TODO: USTA rating + registration code form.</p>
    </div>
  );
}
